# Assistant performance report

Measured 2026-09-15 10:49 on Olexanders-MacBook-Pro (macOS 26.5.2, Arm64, 10 cores). Generated by `dotnet run --project TokkDb.Assistant.TokenBudget -- perf`; do not edit by hand. Every timing names its fixture, whether it is cold or warm, the runs behind it, p50 and p95, and the planner path where one is required (NF-6); a time met by a scan where a seek was required fails.

| requirement | what | fixture | mode | runs | p50 | p95 | limit | planner path | |
|---|---|---|---|---:|---:|---:|---:|---|---|
| NF-3 | open the database and list the conversations and the overview | 100000 records × 8 fields (text, whole number, amount, day, moment, yes/no, text, text), 1000 requests × 3 steps | cold (first open after process start) | 1 | 157 ms | 157 ms | 2.00 s | — | met |
| NF-3 | open the database and list the conversations and the overview | 100000 records × 8 fields (text, whole number, amount, day, moment, yes/no, text, text), 1000 requests × 3 steps | warm | 10 | 66 ms | 89 ms | 2.00 s | — | met |
| NF-2 | retrieve 100 records of one category, oldest first | 100000 records × 8 fields (text, whole number, amount, day, moment, yes/no, text, text), 1000 requests × 3 steps | warm | 10 | 35 ms | 88 ms | 2.00 s | required IndexSeek|RangeWalk; took RangeWalk: one page of the ordered index walk on entries.day | met |
| BR-1a | the overview | 100000 records × 8 fields (text, whole number, amount, day, moment, yes/no, text, text), 1000 requests × 3 steps | warm | 10 | 0 ms | 0 ms | 200 ms | — | met |
| NF-5 | ingest 10000 rows in one transaction | 10000 rows × 5 fields, no key, fingerprint identity | warm | 3 | 2.56 s | 3.08 s | 30.00 s | — | met |

- NF-3, cold (first open after process start): the OS file cache is not cleared: cold is the process's first open.
- NF-2, warm: excluding model time: the query is run as the assistant runs it, after the model wrote it; the first run of a sort over a thing this large raises the index it then walks, which is where a p95 above the p50 comes from.
- BR-1a, warm: pages read: 0 at 100000 records, 0 at 1 000 records - does not grow with the records.
- NF-5, warm: excluding model time: the importer is run as the store flow runs it.

Encryption at rest is out of scope (NF-4e): the engine has none, and the database relies on the user's application data location and its file permissions.
