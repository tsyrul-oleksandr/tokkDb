<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox must be enabled to distribute a MacCatalyst app through the Mac App Store. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>
        <!-- When App Sandbox is enabled, this value is required to open outgoing network connections. -->
        <key>com.apple.security.network.client</key>
        <true/>
        <!-- The sandbox lets a person choose files through the open panel and drop them on the window
             only with this (UI-3). Read-write rather than read-only: the document picker opens what was
             chosen with a read-write sandbox extension, and a read-only grant makes it fail with
             "couldn't issue sandbox extension com.apple.app-sandbox.read-write". Nothing is written. -->
        <key>com.apple.security.files.user-selected.read-write</key>
        <true/>
    </dict>
</plist>

